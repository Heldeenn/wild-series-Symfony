{% extends 'base.html.twig' %}
{% block title %}{{ categoryName }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Catégorie {{ categoryName }}</h1>
            {% for program in programs %}
                <div class="card">
                    <img class="card-img-top w-25" src="{{program.poster}}" alt="{{ program.title }} poster">
                    <div class="card-body">
                        <h2 class="card-title">{{ program.title }}</h2>
                        <p class="card-text">{{ program.summary }}</p>
                    </div>
                </div>
            {% endfor %}
    </div>

{% endblock %}
