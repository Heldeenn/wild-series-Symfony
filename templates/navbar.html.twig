<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Wild-series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link active" href="{{ path('category_add') }}"><span class="sr-only">(current)</span>Categories</a>
            <a class="nav-item nav-link active" href="{{ path('program_index') }}"><span class="sr-only">(current)</span>Séries</a>
            <a class="nav-item nav-link active" href="{{ path('actor_index') }}"><span class="sr-only">(current)</span>Acteurs/Actrices</a>
        </div>
    </div>
</nav>
